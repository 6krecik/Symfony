{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReservationTor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datastart</th>
                <th>Datestop</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reservationtor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dataStart %}{{ entity.dataStart|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.dateStop %}{{ entity.dateStop|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reservationtor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reservationtor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reservationtor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
